"""
UI Tests Package

Browser-based tests using Playwright to verify user interface functionality.
These tests are slower than unit tests but catch UI-specific bugs.

Run with: pytest tests/ui/ -v -m ui
"""
